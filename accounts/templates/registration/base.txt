<nav class="navbar navbar-expand-lg bg-success navbar-dark sticky-top shadow-sm py-3">
        <div class="container">
            <a class="navbar-brand fw-bold" href="{% url 'house_list' %}">SUA Student Housing</a>
            
            <div class="ms-auto">
                {% if request.user.is_authenticated %}
                    <span class="text-white fw-bold me-3">Habari, {{ request.user.username }}!</span>
                    <form method="post" action="{% url 'logout' %}" class="d-inline">
                        {% csrf_token %}
                        <button type="submit" class="btn btn-danger btn-sm rounded-pill fw-bold px-3">Toka (Logout)</button>
                    </form>
                {% else %}
                    <a class="btn btn-outline-light btn-sm rounded-pill fw-bold me-2 pe-4 ps-4" href="{% url 'login' %}">Log In</a>
                    <a class="btn btn-light btn-sm rounded-pill fw-bold text-success pe-3 ps-3" href="{% url 'register' %}">Sign Up</a>
                {% endif %}
            </div>
        </div>
    </nav>